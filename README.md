# noxos-server

NoxOS OTA update server. Custom signed OTA channel using AOSP's `ota_from_target_files` tooling, self-hosted — same pattern as LineageOS/GrapheneOS/CalyxOS.

Part of [NoxOS](https://github.com/parrothacker1/noxos). Status: not yet started (Phase IV of the roadmap).
